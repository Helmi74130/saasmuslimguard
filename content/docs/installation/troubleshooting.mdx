---
title: "Dépannage de l'installation"
description: "Solutions aux problèmes courants lors de l'installation de MuslimGuard"
section: "installation"
order: 4
lastUpdated: "2025-01-24"
keywords: ["dépannage", "erreurs", "problèmes", "installation", "troubleshooting"]
---

# Dépannage de l'installation

Ce guide vous aide à résoudre les problèmes les plus courants rencontrés lors de l'installation de MuslimGuard.

## Problèmes d'installation depuis le Chrome Web Store

### L'extension ne s'installe pas

<Callout type="error">
**Symptôme** : Le bouton "Ajouter à Chrome" ne fonctionne pas ou affiche une erreur.
</Callout>

**Solutions** :

1. **Vérifiez votre version de Chrome**
   ```
   Allez dans chrome://settings/help
   Version minimale requise : 100
   ```

2. **Vérifiez votre connexion Internet**
   - Assurez-vous d'avoir une connexion stable
   - Essayez de recharger la page du Chrome Web Store

3. **Nettoyez le cache Chrome**
   ```
   1. Allez dans chrome://settings/privacy
   2. Cliquez sur "Effacer les données de navigation"
   3. Sélectionnez "Images et fichiers en cache"
   4. Cliquez sur "Effacer les données"
   ```

4. **Essayez en navigation privée**
   - Ouvrez une fenêtre de navigation privée (Ctrl+Shift+N)
   - Accédez au Chrome Web Store
   - Tentez à nouveau l'installation

### L'icône de l'extension n'apparaît pas

<Callout type="warning">
**Symptôme** : L'extension est installée mais l'icône n'est pas visible dans la barre d'outils.
</Callout>

**Solution** :

L'icône peut être masquée par défaut. Pour l'afficher :

1. Cliquez sur l'icône des extensions (pièce de puzzle)
2. Trouvez "MuslimGuard" dans la liste
3. Cliquez sur l'icône de punaise 📌 pour l'épingler

### Erreur de permissions

<Callout type="error">
**Symptôme** : "Cette extension nécessite des permissions que vous n'avez pas accordées"
</Callout>

**Solution** :

1. Allez dans `chrome://extensions`
2. Trouvez MuslimGuard
3. Cliquez sur "Détails"
4. Vérifiez que toutes les permissions sont activées :
   - ✅ Lire et modifier toutes vos données sur les sites que vous visitez
   - ✅ Afficher les notifications
   - ✅ Stocker des données

## Problèmes d'installation manuelle

### "Failed to load extension"

<Callout type="error">
**Symptôme** : Erreur lors du chargement du dossier `dist/` dans Chrome.
</Callout>

**Solutions** :

1. **Vérifiez que le dossier dist existe**
   ```bash
   ls dist/
   # Doit contenir : manifest.json, icons/, scripts/, etc.
   ```

2. **Reconstruisez l'extension**
   ```bash
   rm -rf dist
   pnpm build
   ```

3. **Vérifiez le manifest.json**
   - Assurez-vous qu'il est valide JSON
   - Vérifiez que tous les fichiers référencés existent

### Manifest version non supportée

<Callout type="error">
**Symptôme** : "Manifest version 3 required"
</Callout>

**Solution** :

Mettez à jour votre version de Chrome :
```
chrome://settings/help
```

MuslimGuard utilise Manifest V3, supporté à partir de Chrome 88.

### Erreurs de compilation

<Callout type="error">
**Symptôme** : `pnpm build` échoue avec des erreurs TypeScript ou module.
</Callout>

**Solutions** :

1. **Nettoyez et réinstallez les dépendances**
   ```bash
   rm -rf node_modules pnpm-lock.yaml dist
   pnpm install
   pnpm build
   ```

2. **Vérifiez votre version de Node.js**
   ```bash
   node --version
   # Version requise : 18.0.0 ou supérieure
   ```

3. **Vérifiez les variables d'environnement**
   ```bash
   # Créez un fichier .env.local si manquant
   cp .env.example .env.local
   ```

## Problèmes de connexion à l'extension

### Impossible de se connecter

<Callout type="error">
**Symptôme** : L'extension ne parvient pas à se connecter à votre compte MuslimGuard.
</Callout>

**Solutions** :

1. **Vérifiez votre connexion Internet**
   - Ouvrez https://muslimguard.com dans votre navigateur
   - Assurez-vous que le site est accessible

2. **Vérifiez vos identifiants**
   - Email correct et mot de passe valide
   - Essayez de vous connecter sur le site web d'abord

3. **Désactivez temporairement les autres extensions**
   - Certaines extensions peuvent bloquer les requêtes API
   - Désactivez-les une par une pour identifier le conflit

4. **Vérifiez les paramètres de pare-feu**
   - Assurez-vous que Chrome peut accéder à `api.muslimguard.com`

### Erreur "Extension not linked"

<Callout type="error">
**Symptôme** : "Cette extension n'est pas liée à votre compte"
</Callout>

**Solution** :

1. Cliquez sur l'icône MuslimGuard
2. Cliquez sur "Se connecter"
3. Autorisez l'extension à accéder à votre compte
4. Vérifiez dans les paramètres que l'extension apparaît comme "Liée"

## Problèmes de synchronisation

### Les paramètres ne se synchronisent pas

<Callout type="warning">
**Symptôme** : Les règles de blocage ou paramètres ne sont pas synchronisés entre appareils.
</Callout>

**Solutions** :

1. **Vérifiez votre abonnement**
   - Allez dans Paramètres > Abonnement
   - Assurez-vous d'avoir un abonnement actif

2. **Forcez la synchronisation**
   - Cliquez sur l'icône MuslimGuard
   - Paramètres > Synchronisation > "Synchroniser maintenant"

3. **Reconnectez l'extension**
   - Déconnectez-vous de l'extension
   - Reconnectez-vous avec vos identifiants

## Problèmes de performance

### L'extension ralentit Chrome

<Callout type="warning">
**Symptôme** : Chrome devient lent après l'installation de MuslimGuard.
</Callout>

**Solutions** :

1. **Vérifiez l'utilisation des ressources**
   - Allez dans Chrome Task Manager (Shift+Esc)
   - Vérifiez la consommation de MuslimGuard

2. **Réduisez le nombre de règles de blocage**
   - Trop de règles peuvent ralentir l'extension
   - Gardez uniquement les règles essentielles

3. **Désactivez le scanner de mots-clés sur certains sites**
   - Le scanner peut être intensif sur les pages volumineuses
   - Ajoutez des exceptions pour les sites de confiance

## Désinstallation et réinstallation

Si les problèmes persistent, essayez une réinstallation complète :

### Désinstallation propre

1. **Sauvegardez vos paramètres**
   - Paramètres > Sauvegarde > "Exporter les paramètres"

2. **Désinstallez l'extension**
   - Allez dans `chrome://extensions`
   - Trouvez MuslimGuard
   - Cliquez sur "Supprimer"

3. **Nettoyez les données résiduelles**
   - Allez dans `chrome://settings/privacy`
   - Effacez les données de navigation (dernière heure)

4. **Réinstallez l'extension**
   - Redémarrez Chrome
   - Réinstallez depuis le Chrome Web Store

5. **Restaurez vos paramètres**
   - Paramètres > Sauvegarde > "Importer les paramètres"

## Obtenir de l'aide supplémentaire

Si votre problème persiste après avoir essayé ces solutions :

### Support communautaire

- **Forum** : [community.muslimguard.com](https://community.muslimguard.com)
- **Discord** : [discord.gg/muslimguard](https://discord.gg/muslimguard)

### Support technique

- **Email** : [support@muslimguard.com](mailto:support@muslimguard.com)
- **Documentation API** : [/docs/api](/docs/api)
- **FAQ** : [/faq](/faq)

### Rapporter un bug

Pour signaler un bug, incluez :

1. **Version de l'extension** (visible dans `chrome://extensions`)
2. **Version de Chrome** (visible dans `chrome://settings/help`)
3. **Système d'exploitation** (Windows, Mac, Linux)
4. **Description détaillée du problème**
5. **Étapes pour reproduire**
6. **Captures d'écran** (si applicable)

<Callout type="info">
Envoyez vos rapports de bugs à [bugs@muslimguard.com](mailto:bugs@muslimguard.com) ou créez une issue sur notre [dépôt GitHub](https://github.com/muslimguard/extension/issues).
</Callout>
